#include "main.h"
#include "utils.h"

using namespace std;

int main() {

    int initial_state[16] = {1, 12, 11, 10, 2, 13, 8, 9, 3, 14, 15, 0, 4, 5, 6, 7};

    // How to create a new node:
    // Node n(initial_state, Direction::UP, 0, 1);

    return 0;
}